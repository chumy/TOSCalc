import { ImgJugadorPipe } from './img-jugador.pipe';

describe('ImgJugadorPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgJugadorPipe();
    expect(pipe).toBeTruthy();
  });
});
